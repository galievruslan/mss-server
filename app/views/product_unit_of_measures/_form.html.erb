<%= form_for([@product, @product_unit_of_measure]) do |f| %>
  <% if @product_unit_of_measure.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@product_unit_of_measure.errors.count, "error") %> prohibited this product_unit_of_measure from being saved:</h4>

      <ul>
      <% @product_unit_of_measure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :unit_of_measure_id, 'Unit of measure' %>
    <%= f.select(:unit_of_measure_id, @unit_of_measures.collect {|p| [p.name, p.id]}, :include_blank => "Select a unit of measure") %>
  </div>
  <div class="field">
    <%= f.label :count_in_base_unit %>
    <%= f.number_field :count_in_base_unit, :min => 0, :step => 0.01 %>
  </div>
    <div class="field form-inline">    
    <%= f.check_box :base %>
    <%= f.label :base , 'Base unit of measure'%><br /><br />
  </div>
  <div class="actions">
    <%= f.submit(class: 'btn') %>
  </div>
<% end %>
