<p>
  <b>External Key:</b>
  <%= @product.external_key %>
</p>
<p>
  <b>Name:</b>
  <%= @product.name %>
</p>
<h4>Product unit of measures</h4>
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Product unit of measure name</th>
      <th>Count in base unit</th>
    </tr>
  </thead>
  <tbody>
    <% @product.product_unit_of_measures.each do |product_unit_of_measure| %>  
    <tr <%='class=success' if product_unit_of_measure.base %>>
      <td><%= product_unit_of_measure.unit_of_measure.name %></td>
      <td><%= product_unit_of_measure.count_in_base_unit %></td>
    </tr>
    <% end %>
  </tbody>
</table>
* Base unit of measure marked in green<br /><br />
<h4>Product price list</h4>	
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Price name</th>
      <th>Price</th>
    </tr>
  </thead>
  <tbody>
    <% @product.price_list_lines.each do |price_list_line| %>  
    <tr>
      <td><%= price_list_line.price_list.name %></td>
      <td><%= price_list_line.price %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
