<p>
  <b><%= t(:name)%>:</b>
  <%= @status.name %>
</p>
<p>
  <b><%= t(:validity)%>:</b>
  <%= @status.validity ? t(:truth) : t(:lie) %>
</p>

<% if can? :manage, Status %><%= link_to t(:edit), edit_status_path(@status) %> |<% end %>
<%= link_to t(:back), statuses_path %>