<%= form_for([@route, @route_point]) do |f| %>
  <% if @route_point.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@route_point.errors.count, "error") %> prohibited this route_point from being saved:</h4>

      <ul>
      <% @route_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shipping_address_id, t(:shipping_address) %>
    <%= f.select(:shipping_address_id, @shipping_addresses.collect {|p| [p.name, p.id]}, :include_blank => t(:select_shipping_address)) %>
  </div>
  <div class="field">
    <%= f.label :status_id, t(:status) %>
    <%= f.select(:status_id, @statuses.collect {|p| [p.name, p.id]}, :include_blank => t(:select_status)) %>
  </div>
  <div class="actions">
    <%= f.submit(t(:save), class: 'btn') %>
  </div>
<% end %>
