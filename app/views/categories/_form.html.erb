<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h4>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent_id %>
    <%= f.select(:parent_id, @categories.collect  {|p| [ p.name, p.id ] }, :include_blank => "Select a category") %>
    
  </div>
  <div class="field">
    <%= f.label :external_key %>
    <%= f.text_field :external_key %>
  </div>
  <div class="field form-inline">
  	<%= f.check_box :validity %>
    <%= f.label :validity %><br /><br />
  </div>
  <div class="actions">
    <%= f.submit(class: 'btn') %>
  </div>
<% end %>
