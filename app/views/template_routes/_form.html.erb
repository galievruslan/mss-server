<%= form_for(@template_route) do |f| %>
  <% if @template_route.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@template_route.errors.count, "error") %> prohibited this template_route from being saved:</h4>

      <ul>
      <% @template_route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :manager_id, 'Manager' %>
    <%= f.select(:manager_id, @managers.collect  {|p| [ p.name, p.id ]}, :include_blank => "Select a manager") %>
  </div>
  <div class="field">
    <%= f.label :day_of_week %>
    <%= f.select(:day_of_week, @days_of_week.collect  {|p| [ day_name(p), p ]}, :include_blank => "Select a day of week") %>
  </div>
  <div class="actions">
    <%= f.submit(class: 'btn') %>
  </div>
<% end %>
